<Window x:Class="Taschenrechner_WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Taschenrechner_WPF"
        mc:Ignorable="d"
        Title="MainWindow" Height="800" Width="400">
    
    <Window.Resources>
        <!-- Style für ALLE Buttons -->
        <Style TargetType="Button">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Background" Value="#FF1E88E5"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderBrush" Value="Black"/>
            <Setter Property="Opacity" Value="0.8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="10"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Opacity" Value="0.8"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="TextAlignment" Value="Right"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="BorderBrush" Value="Black"/>

            
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10"
                                SnapsToDevicePixels="True">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    
    <Grid Background="DarkSlateGray">
        <!-- Zeilen -->
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/> <!-- 0 TextBox -->
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Spalten -->
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- TextBox Anzeige -->
        <TextBox Opacity="0.8" BorderBrush="Black" x:Name="Display" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="4"
                 FontSize="24" IsReadOnly="True" TextAlignment="Right" Margin="10"/>
        

      
        
        <!-- Zahlen Buttons -->
        <Button Content="1" Grid.Row="2" Grid.Column="0" Click="Button_Click"/>
        <Button Content="2" Grid.Row="2" Grid.Column="1" Click="Button_Click"/>
        <Button Content="3" Grid.Row="2" Grid.Column="2" Click="Button_Click"/>
        <Button Content="4" Grid.Row="3" Grid.Column="0" Click="Button_Click"/>
        <Button Content="5" Grid.Row="3" Grid.Column="1" Click="Button_Click"/>
        <Button Content="6" Grid.Row="3" Grid.Column="2" Click="Button_Click"/>
        <Button Content="7" Grid.Row="4" Grid.Column="0" Click="Button_Click"/>
        <Button Content="8" Grid.Row="4" Grid.Column="1" Click="Button_Click"/>
        <Button Content="9" Grid.Row="4" Grid.Column="2" Click="Button_Click"/>
        <Button Content="0" Grid.Row="5" Grid.Column="1" Click="Button_Click"/>


        <!-- Operatoren -->
        <Button Content="+" Grid.Row="2" Grid.Column="3" Click="Operator_Click"/>
        <Button Content="-" Grid.Row="3" Grid.Column="3" Click="Operator_Click"/>
        <Button Content="*" Grid.Row="4" Grid.Column="3" Click="Operator_Click"/>
        <Button Content="/" Grid.Row="5" Grid.Column="3" Click="Operator_Click"/>
        <Button Content="=" Grid.Row="5" Grid.Column="2" Click="Equals_Click"/>
        <Button Content="C" Grid.Row="5" Grid.Column="0" Click="Clear_Click"/>

    </Grid>

</Window>
